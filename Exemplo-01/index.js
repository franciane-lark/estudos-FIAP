//Cálculo área de um triângulo
let calculo, base, altura;

base = prompt("Digite a base:");
altura = prompt("Digite a altura:");

calculo = (base*altura)/2;

console.log("A área do Triângulo é:", calculo);